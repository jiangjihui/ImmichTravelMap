{
  "name": "immich-travel-map",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:server": "npm run build:packages && npm --prefix apps/server run dev",
    "dev:web": "npm run build:packages && npm --prefix apps/web run dev",
    "dev:desktop": "npm run build && npm --prefix apps/desktop run dev",
    "build:packages": "npm --prefix packages/shared-types run build && npm --prefix packages/track-core run build",
    "build:server": "npm --prefix apps/server run build",
    "build:web": "npm --prefix apps/web run build",
    "build:desktop": "npm run build && npm --prefix apps/desktop run build",
    "build": "npm run build:packages && npm run build:web && npm run build:server",
    "start": "npm --prefix apps/server run start",
    "release:check": "node scripts/check-release-artifacts.mjs",
    "release:build": "npm run build && npm run release:check",
    "release:start": "npm run release:build && npm run start"
  }
}
