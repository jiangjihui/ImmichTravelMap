[package]
name = "immich-travel-map-desktop"
version = "0.1.0"
description = "Immich Travel Map desktop shell"
authors = ["ImmichTravelMap"]
edition = "2021"
build = "build.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
