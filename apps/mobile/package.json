{
  "name": "immich-travel-map-mobile",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build:web": "npm --prefix ../web run build",
    "copy": "npm run build:web && cap copy",
    "sync": "npm run build:web && cap sync",
    "add:android": "cap add android",
    "add:ios": "cap add ios",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "typescript": "^5.7.2"
  }
}
